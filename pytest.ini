[pytest]
# Both suites, so a bare `pytest` matches CI. testpaths listed only
# `tests` while CI passed `scripts/tests/` explicitly, so neither ever
# ran the other's cases.
testpaths = tests scripts/tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
